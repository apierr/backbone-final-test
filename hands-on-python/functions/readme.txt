16/4/2013
Scrivere delle funzioni in Python che realizzino le seguenti operazioni:

1) il calcolo del valore assoluto di un dato numero reale;
2) la somma, la media e la varianza di una lista di numeri;
3) un dizionario con l'equivalente in ore, minuti e secondi di un dato numero di secondi;
4) un dizionario con il numero di occorrenze di ciascuna lettera di una data parola;
5) il massimo comune divisore di due dati numeri interi;
6) data una parola composta da un qualsiasi numero di caratteri, determinare se sia palindroma
7) il numero di Fibonacci associato ad un dato numero intero positivo
